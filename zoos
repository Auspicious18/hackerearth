
// Sample code to perform I/O:
#include <stdio.h>

int main()
{
    int i,p=0,o=0;
	char s[20];
	scanf("%s", s);
	for(i=0;i<strlen(s);i++)
	{
	    if(s[i]=='z')
	        p=p+1;
	    else 
	        o=o+1;
	}
	if(o==2*p)
	printf("Yes");
	else
	printf("No");
}
