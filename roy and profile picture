#include<stdio.h>
int main()
{
	int W,H,L,n,i;
	scanf("%d",&L);
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
	
	scanf("%d %d",&H,&W);
	if(H<L||W<L)
		printf("UPLOAD ANOTHER\n");
	else if(W>=L&&H>=L)
		{
			if(W==H)
				printf("ACCEPTED\n");
			else
				printf("CROP IT\n");
		}
	
	}
}
