#include <stdio.h>
#include <string.h>
int main()
{
	char s[100];
	int p,i;
	scanf("%s",s);
	p=strlen(s);
	for(i=0;i<p;i++)
	{
		if(s[i]>=97&&s[i]<=122)
			s[i]=s[i]-32;
		else
			s[i]=s[i]+32;
	}
	printf("%s",s);
}
